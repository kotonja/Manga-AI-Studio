{
  "name": "manga-ai",
  "private": true,
  "version": "0.1.0",
  "description": "Production-grade AI manga creation studio monorepo.",
  "workspaces": [
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "dev": "docker compose up --build",
    "down": "docker compose down",
    "test": "sh scripts/test-all.sh",
    "test:api": "docker compose run --rm --no-deps -e ENABLE_BACKGROUND_JOBS=false api sh -lc \"python -m pip install -e /app/services/api[test] >/dev/null && pytest\"",
    "typecheck:web": "pnpm --filter @manga-ai/web typecheck",
    "build:web": "pnpm --filter @manga-ai/web build",
    "seed:demo": "sh scripts/seed-demo.sh"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
